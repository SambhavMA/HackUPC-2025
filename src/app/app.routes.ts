import { Routes } from '@angular/router';
import { RecomendationsComponent } from './recomendations/recomendations.component';

export const routes: Routes = [
    { path: '', component: RecomendationsComponent}
];
